enum HomeOptions { upcoming, score, favorites }

enum DetailsOptions { summary, lineUp, stats, h2H, standings }

enum BrowsingOptions { top, region, favorites }

enum SearchTypes { team, league }

enum EventType {
  goal,
  normalGoal,
  ownGoal,
  penaltyGoal,
  penaltyMissed,
  card,
  yellowCard,
  redCard,
  substitution,
  varCheck,
  varGoalCancelled,
  varPenaltyConfirmed
}
